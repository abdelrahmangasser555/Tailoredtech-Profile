# Pixel Scoreboard

Open `index.html`. Plus and minus should change the numbers.

1. Rename the teams in `index.html`
2. Make the numbers huge in `styles.css`
3. Minus must not go below 0 in `app.js`

Leave it 3-2 and screenshot.
