# Harbor Log

Open `index.html`. Type a line. Hit save.

1. Rename the ship in `index.html`
2. Make the page look like paper in `styles.css`
3. Make save append a list item in `app.js`

Screenshot three log lines when it works.
