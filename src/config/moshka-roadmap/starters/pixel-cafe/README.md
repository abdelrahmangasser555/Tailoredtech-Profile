# Pixel Cafe

Open `index.html` in Chrome. Then open the files in an editor.

1. Change a drink name in `index.html`
2. Change a color in `styles.css`
3. Make a click add to the total in `app.js`

No npm. No install. Show Abdelrahman a screenshot when the total works.
