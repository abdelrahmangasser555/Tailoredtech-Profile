# Local content editor (dev only)
# LOCAL_EDIT=true
# NEXT_PUBLIC_LOCAL_EDIT=true

# Finance module gate (4-digit PIN hashed with Node scrypt)
# Generate: node -e "const {scryptSync,randomBytes}=require('crypto');const s=randomBytes(16).toString('hex');const h=scryptSync('YOUR_PIN',s,64).toString('hex');console.log(s+':'+h)"
FINANCE_PASSWORD_HASH=
FINANCE_SESSION_SECRET=
